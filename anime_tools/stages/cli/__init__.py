"""Thin argparse shells over ``anime_tools.stages``. Each is runnable as
``python -m anime_tools.stages.cli.<name>``; the trainer's ``make caption-*`` /
``make preprocess-captions`` targets wrap them."""
