node_modules/

# Copied into anime_tools/gui/static/index.html verbatim by build.ts (only the
# <script src> tag is rewritten), so reformatting it churns the committed bundle.
index.html

# Hand-compacted on purpose: one rule per line, so the whole theme reads as a
# table and design/boards can quote line-for-line. Prettier expands it 5x.
src/styles.css

# `scripts/wrap_md.py` owns markdown instead (pre-commit runs it): prettier's
# `proseWrap` is a greedy fill, so editing one sentence re-wraps every line after
# it, and it renames emphasis markers (*x* -> _x_) for no gain. See CLAUDE.md.
*.md
