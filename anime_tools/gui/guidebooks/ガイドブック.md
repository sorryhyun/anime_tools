# anime_tools ガイドブック

このドキュメントは、anime_tools でデータセットをキュレーションするための最初から最後までの手引きです。
インストールし、画像を配置し、Web GUI を開き、キャプション / グループ化 / マスクの各ステージを順に実行して、
結果をそのまま学習に使えるデータセットとして発行するところまでを扱います。
ワンラインインストーラーでインストールし、コードを一度も開いたことのない方に向けて書かれています。
各ステージが何を読んで何を書くのか、実行をどう取り消すのかを説明します。
ノブそのものは各ステージのフォーム上にあり（そのステージ自身の `--help` から生成されます）、
個々の説明は各セクションからリンクされている設計ドキュメントにあります。

---

## 目次

1. [anime_tools とは](#1-anime_tools-とは)
2. [動作要件](#2-動作要件)
3. [インストール](#3-インストール)
4. [キュレーションホームとそのレイアウト](#4-キュレーションホームとそのレイアウト)
5. [Hugging Face サインインとモデル](#5-hugging-face-サインインとモデル)
6. [GUI](#6-gui)
7. [ステージごとのワークフロー](#7-ステージごとのワークフロー)
8. [CLI での同等コマンド](#8-cli-での同等コマンド)
9. [アップデート](#9-アップデート)
10. [トラブルシューティング](#10-トラブルシューティング)
11. [追加資料](#11-追加資料)

---

## 1. anime_tools とは

`anime_tools` はアニメ拡散モデル学習のためのデータセット・キュレーションツールです。
手書きキャプション付きの画像フォルダを受け取り、学習が読むもの一式、すなわちバケットリサイズ済み画像、
補正・拡充されたキャプション、キャプションバリアント、型付きタグインデックス、学習用マスク、
重複を間引くためのグループ化マニフェストを生成します。

インストールは単体で完結し、DiT も VAE も学習スタックの類も要りません。
書き出すものはすべてディスク上のただのファイルなので、生成したデータセットはどのトレーナーにも渡せます。

---

## 2. 動作要件

| 項目 | 備考 |
|---|---|
| Python | 3.13。インストーラーが固定します。マシンに無ければ `uv` が取得します。 |
| GPU | 任意ですが強く推奨します。すべてのモデルステージ（タガー、SAM3、PE-Spatial、OCR）は CPU でも動きますが、速度は数分の一です。各ステージは torch が CUDA を認識すれば CUDA を、そうでなければ CPU を選びます。 |
| OS | Linux、Windows、macOS。macOS には CUDA も triton もありませんが、パッケージ側が SAM3 のその両方への依存を肩代わりするので、macOS でも SAM3 ステージは CPU で動きます。 |
| ディスク | 各ステージが初回使用時に取得するモデル（タガーのバックボーン、SAM3、PE-Spatial、OCR、Danbooru タグ KB）に加え、`workspace/` 以下のデータセットのリサイズ済みコピー。 |

> PyPI の Windows 向け torch は CPU 専用です。そのため PowerShell インストーラーはデフォルトで
> CUDA 13.0 の torch インデックスを使います（§3）。PyPI の Linux 向け torch は最初から CUDA を同梱しています。

---

## 3. インストール

### 3.1 ワンライン、チェックアウト不要

Linux / macOS:

```bash
curl -fsSL https://github.com/sorryhyun/anime_tools/releases/latest/download/install.sh | sh
```

Windows (PowerShell):

```powershell
irm https://github.com/sorryhyun/anime_tools/releases/latest/download/install.ps1 | iex
```

どちらも [uv](https://astral.sh/uv) が無ければインストールしたうえで、最新リリースを torch と SAM3 込みで
専用の仮想環境に `uv tool install` し、`anime-tools-gui` を PATH に置きます。
git のチェックアウトも CUDA ツールキットも不要です。torch の wheel が CUDA ランタイムを同梱しています。

インストーラーは 2 つの環境変数で制御できます:

| 変数 | 効果 |
|---|---|
| `ANIME_TOOLS_VERSION=v0.3.1` | 最新リリースの代わりにそのタグをインストールします。 |
| `TORCH_INDEX=https://download.pytorch.org/whl/cu128` | torch 用の追加パッケージインデックス。PowerShell インストーラーは cu132（50 番台のカードに必要なチャンネル）をデフォルトにしています。古い CUDA ランタイムを使う場合はこれを、CUDA なしなら `…/whl/cpu` を指定します。Linux では CPU 専用ビルドにだけ必要です。 |

完了したら、PATH の変更が反映されるように新しいシェルを開いてから:

```bash
cd <your dataset folder>
anime-tools-gui --open
```

### 3.2 自分のプロジェクトの依存関係として

```bash
uv add "anime-tools @ git+https://github.com/sorryhyun/anime_tools"
```

PyPI パッケージはありません。タグを固定するか、`[tool.uv.sources]` をチェックアウトに向けてください。
自分の `pyproject.toml` にコピーしておくべきものが 1 つあります。
SAM3 は `numpy<2` を固定していますが、この固定は古くなっています。
このリポジトリはそれをオーバーライドしていますが、uv が `[tool.uv]` を見るのはワークスペースのルートだけなので、
anime-tools に依存するプロジェクトは同じオーバーライドを書き直さないと numpy の解決に失敗します:

```toml
[tool.uv]
override-dependencies = ["numpy>=2.0"]
```

上のインストーラーは、同じオーバーライドをコマンドラインで渡しています。

### 3.3 ツールインストールから CLI を実行する

PATH 上にあるコマンドは `anime-tools-gui` だけです。
ターミナルからステージを実行するには、ツールの環境の中で Python を実行します:

```bash
uv tool run --from anime-tools python -m anime_tools.downloads --list
```

---

## 4. キュレーションホームとそのレイアウト

すべては 1 つのディレクトリ、キュレーションホームからの相対です。優先順位の順に、`ANIME_TOOLS_HOME`、
次に `ANIMA_HOME`（この変数も参照されるので、その下にすでに用意したツリーはそのまま使えます）、次に実行したディレクトリです。
`anime-tools-gui --home <dir>` はそのサーバーに対してこれを設定します。

```
<home>/
  (source_dir)/                   INPUT  — your images + hand-written captions; the tools only read it
  workspace/                      everything the tools produce
    resized/<rel>.png               bucket-resized image — the tree every stage reads
    resized/<rel>.txt               the revised caption (stage output)
    resized/<rel>.history.txt       what that caption used to say, one line per version
    resized/<rel>.variants.txt      shuffle / dropout variants, v0 = pristine
    ocr/<rel>.ocr.txt               words in the picture (OCR); not a caption
    masks_sam/                      the SAM3 mask generator's own tree
    masks/<rel>/{stem}_mask.png     the merge — what Export publishes
    captions/<stage>/report.json    what each run did, and how to undo it
    groups/groups.json              the grouping manifest
    _excluded/excluded.json         images you took out of the pipeline
    _excluded/{resized,masks,ocr}/    and the files that moved out with them
  (export_target_dir)/            OUTPUT — the published dataset; written only by Export
  models/                         model weights (ANIME_TOOLS_MODELS overrides)
```

`workspace/` の外にある 2 つの名前は既定値にすぎません。それぞれ `src` と `out` の
データセットルートで、⚙ Settings（§6.4）から手持ちのフォルダーに向けられます。

目にするものの大半は、3 つのルールで説明できます:

- `(source_dir)/` はステージから見て読み取り専用です。そこに置いたキャプションが*マスター*になります。
  すべてのステージは代わりに `workspace/resized/` 以下に*改訂版*キャプションを書き、
  改訂版がまだ無いときはマスターにフォールバックします。マスターを書くものは 2 つしかありません。
  GUI のキャプションエディターでその段を手で編集したときと、Export です。
- 画像を開くステージは、マスキングもグループ化も含めて、すべて `workspace/resized/` 以下の画像を開きます。
  そのためパイプライン全体が 1 つのジオメトリを共有します。`(source_dir)/` にしか無い画像は、どのステージからも見えません。
  GUI がそれを必要とするステージの前に自動で Resize を実行するのはこのためです（§7.1）。
- Export 以外は `workspace/` の外に何も書きません（§7.9）。ワークスペースは削除して再生成できます。
  ステージによってマスターキャプションを失うことはありません。
- `workspace/_excluded/` は意図して作る例外です。データセットから外した画像がここに入ります（§6.5）。
  その中身はどのステージも読まず、Export は `(export_target_dir)/_excluded/` の下に発行します。
  トレーナーが読むツリーの*中*ではなく、その*隣*です。

画像ファイルはステムでキャプションと対応付けられます: `chars/alice/001.png` は `chars/alice/001.txt` を読みます。
サブフォルダはパイプライン全体を通してそのまま保たれます（上記の `<rel>`）。

> ワークスペース導入前のインストールからのアップグレード（`(export_target_dir)/resized/` に直接書いていたもの）:
> `python -m anime_tools.workspace.migrate` は何を移動するかを表示し、`--apply` で実際に移動します。
> ディレクトリをリネームするだけで、マージは決して行いません。移動先がすでに存在すれば報告してスキップします。
> ⚙ Settings で古いパスに固定していたデータセットルートは、名前が挙がるだけで書き換えられません。
> そこはご自身でクリアしてください。

---

## 5. Hugging Face サインインとモデル

モデルのうち 2 つは Hugging Face Hub 上でゲーテッドです。
dbv4 タガーのバックボーン（GPL-3.0。決して同梱せず、ロード時にお使いのトークンで取得します）と SAM 3 です。
どちらも read 権限のトークンが必要で、そのトークンのアカウントは各リポジトリの利用規約に一度同意している必要があります。

GUI では: ☰ → ⚙ Settings… → Hugging Face → トークンを貼り付けます。
トークンは `huggingface_hub` のログインに渡されてそこに保存され、二度と表示されません。
設定されるまでヘッダーには ⚠ no HF token と表示されます。
Models リストのゲーテッド行には accept the terms リンクがあります。
同じアカウントでサインインした状態で開いてください。

事前に取得しておく必要はありません。すべてのステージは必要なものを初回使用時に取得します。
Models & weights ダイアログ（☰ → Models & weights）は、チェックポイントごとに 1 行を用途・
インストール済みかどうか・置き場所とともに一覧し、まとめてインストールされる単位であるパック
（Tagger、Danbooru tag DB、Masking、OCR、Grouping）ごとに並べます。
パックの見出しごとに Download pack ボタン、行ごとに Download ボタン、下部バーに
Download all N missing があります。
ダウンロードは通常のジョブとして一度に 1 つずつ実行され、ステージとスロットを共有します。
これらのボタンは、待ち時間と、ゲーテッドリポジトリで拒否された場合のその通知を、
こちらが選んだタイミングにずらすだけのものです。

ターミナルからは:

```bash
python -m anime_tools.downloads --list        # every row under its pack: installed / MISSING, repo, destination
python -m anime_tools.downloads               # fetch every missing one
python -m anime_tools.downloads sam3 ocr      # fetch by row id or pack id
```

重みは `<home>/models/` 以下に置かれます（`ANIME_TOOLS_MODELS` で上書き可能）。
例外は SAM3 の被写体ソフトプロンプトで、`networks/calibration/` にあります。

---

## 6. GUI

```bash
anime-tools-gui --open              # http://127.0.0.1:8790, opens the browser
anime-tools-gui --home ~/datasets/x # a different curation home
anime-tools-gui --host 0.0.0.0      # expose on the LAN (a headless GPU box)
```

ポートが使用中なら次の空きポートが使われ、表示されます。`--host 0.0.0.0` には**認証がありません**。
前段には自前のトンネルを置いてください。サーバー自体はモデルを一切ロードしません。
すべてのステージは `python -m …` サブプロセスとして一度に 1 つずつ実行され、その出力がパネルにストリーミングされます。

タイトルバーのデータセット名の横、そしてプレビュー上の画像名の横にある ↗ は、そのフォルダーや
ファイルを OS のファイルマネージャーに渡します。フォルダーは開き、ファイルはそのフォルダーの中で
選択された状態で表示されます（画像ビューアーで開くのではありません）。ウィンドウはサーバーが
動いている機械に出るので、ブラウザーがサーバーと同じ機械にあるときだけ描かれます。LAN 越しでは
↗ はありません。

### 6.1 サイドバーはデータセットそのもの

`(source_dir)/` 以下のすべての画像がフォルダ構成のまま並び、行のドットで何が存在するかを示します:
リサイズ済み、マスクあり、そしてキャプションの段ごとに 1 つのドットです。
各画像の下では、そのキャプションが古いものから順にラダーを成します:

| 段 | ファイル | 編集可 |
|---|---|---|
| master | `(source_dir)/<rel>.txt`。手書きで、ステージは読むだけです | はい |
| history (`revised@1`, `revised@2`, …) | `<rel>.history.txt`。改訂版キャプションを置き換えた実行それぞれの直前に、そのキャプションが何と言っていたか | いいえ |
| revised | `workspace/resized/<rel>.txt`。ステージの出力で、次の実行がこれを書き換え、このテキストをバージョンとして残します | はい |
| variants (`v0`, `v1`, …) | `<rel>.variants.txt`。生成物で、`v0` は手つかずの改訂版キャプションです | いいえ |

filter ボックスでツリーを絞り込めます。`↑`/`↓` または `j`/`k` で画像を移動し、
URL の `#<rel>|<kind>` は 1 つのキャプションへのリンクです。tree / groups トグルは同じ一覧を
2 つの順序で描きます。フォルダ順と、Groups ステージが見つけた近縁（near-twin）グループ順です（§7.7）。
取り消し線が引かれ `⊘` が付いた行は、除外した画像です（§6.5）。

### 6.2 キャプションエディター

画像を選択すると、その画像（ソース / マスク / オーバーレイ）がキャプションの横に表示されます。
エディターは 1 つで、バージョンごとにバッジが付きます。タグバッグと各位置節はテキスト内でボックスに囲まれます。
ボックスはサーバー自身のパーサーに由来するため、ブラウザがキャプションの構造を推測することはありません。
タグをダブルクリックすると Danbooru タグ KB で調べられます（ダウンロード後）。

Save（⌘/Ctrl+Enter）は `master` または `revised` を書き込みます。他の段は読み取り専用です。
すべての書き込みは、手動でもステージでも同様に、置き換えたテキストを history の段に積みます。
保存すると次に何をすべきかが表示されます。下流でのテキスト埋め込みの再エンコードは常に必要で、
すでに `.variants.txt` があった改訂版キャプションを編集した場合は、そのサイドカーが古くなるため
Correct の再実行も必要です。

キャプションの下には、OCR ステージが読んだ画像であれば画像内のテキストが、
各行の信頼度と位置とともに表示されます（§7.6）。

バージョンのバッジの後ろには、位置ステージ（§7.4）が見た画像に **分析** バッジが付きます。
これはキャプションではありません。選ぶとエディターの代わりにステージが見たものが表示されます。
ステージが切り出したインスタンスマスクを、リサイズ済み画像の上に被写体ごとに 1 色で塗り、
各色の横にその位置と、節が受け取ったタグ（または画像をスキップした理由）を並べます。
マルチビュー監査のフェーズがその画像を見ていれば、その判定、根拠、マスクも表示されます。
この記録は位置レポートの横に画像ごとに保存されるため、別の画像に対する実行はこれに触れず、
この画像に対する次の実行がこれを置き換えます。

### 6.3 ドックはステージランナー

下端のボタン列がステージ一覧そのものです。Resize は舞台裏にあり、ボタンは
自動タグ · キャプション整備 · OCRタグ付け · グループ化 · マスク · エクスポート です。
キャプション整備は 2 つのステージ（人物位置タグ付け / タグ補正）を、マスクも 2 つ（設定 / 統合）を抱えており、
パネル内で選びます。1 回クリックするとステージのフォームが開き、開いているものをもう一度クリックするとドックが折りたたまれます。

フォームはステージ自身の `--help` から生成されます。開いたときに見えるのは実行ごとに変えたくなるノブで、
残りは各グループの末尾の ▸ advanced (n) に折りたたまれ、隠れたフィールドがデフォルトから外れていれば注記が付きます。
データセットルート、レポートルート、モデルパスがフォームに現れることはありません。これらは ⚙ Settings から来ます。

- Run は選択中の画像だけを対象にステージを実行します（just `<rel>`）。
- Run batch は Settings の `path_pattern` が指すすべての画像に対して実行します。`*` はデータセット全体です。
- Undo はこのステージの直前の実行が書いたものを、そのレポートを逆再生して元に戻します。
- Cancel は実行中のジョブを停止します。

パネルを日本語にすると、これらのボタンは 実行 / 一括実行 / 元に戻す / 中止、☰ メニューの項目は
⚙ 設定… / モデルと重み / 言語 と表示されます。本書では英語名をそのまま使います。

Run は実際に書き込みます。GUI には Apply ゲートがありません。
実行が置き換えたものはキャプションのバージョンバッジ（`revised@2`）になり、
実行のレポートはキャプションパネルで画像ごとの差分として読み返され、
Undo はそのレポートを 2 つのテキストを入れ替えて再生したものです。
Undo にはガードがあります。実行後に手で編集したキャプションはそのまま残され、スキップとして数えられます。

最新の出力行はステージバーに表示され、log ボタンで全体を開けます。
画像を走査するステージはプログレスバーを表示し、前にプリフライトが付く実行は先に step 1/2 · resize を表示します。

### 6.4 ⚙ Settings は 3 つのダイアログ

| ダイアログ | 内容 |
|---|---|
| Settings | キュレーションホームとモデルディレクトリ、5 つのデータセットルート（`src`、`master`、`dst`、`masks`、`out`）、Hugging Face トークン。 |
| Advanced settings | それを受け取るすべてのステージに埋め込まれるステージデフォルト（`path_pattern`、タガーディレクトリ、SAM3 チェックポイントとソフトプロンプト、レポートルート、マスクルート）と、Preprocess ブロック（Resize にはドックボタンが無いため、ここに置かれた Resize ステージ自身のフォーム）。 |
| Models & weights | §5 のモデル行。 |

各ダイアログは自分が持つものだけを保存します。ルートはキュレーションホームからの相対で、存在しないものはフラグが立ちます。
パネルは保存された設定が指すどのルートも読める一方、ディレクトリを作るのはホーム以下に限られるため、
外部ルートのタイプミスは新しい空ディレクトリではなく、存在しないルートになります。

☰ メニューの Language 行はパネルを英語・韓国語・日本語・中国語の間でその場で切り替えます。
初回訪問時はブラウザ自身の言語リストに従います。翻訳されるのはパネルのクローム部分だけです。
ステージのタイトル、フォームのラベルとヘルプはその `--help` から来るため英語のままですし、
キャプション、タグ、パスはそもそもデータです。

---

### 6.5 画像をパイプラインから外す

画像名の横の `⊘ exclude` ボタンは、何も削除せずにその画像をデータセットから外します。
リサイズ済みコピー・マスク・OCR サイドカーが `workspace/_excluded/` に移動し、画像は
そこの台帳に記録されます。それ以降は:

- Resize が飛ばすので、`workspace/resized/` に戻ってくることはありません。
- 他のすべてのステージはそのツリーを歩くので、この画像を見ることもありません。
- Export は `resized/` ではなく `(export_target_dir)/_excluded/` の下に発行するため、
  トレーナーは読みませんが、ファイル自体は手元に残ります。

`(source_dir)/` の元画像と手書きのマスターキャプションには**触れません**。同じボタンの
`↩ put back` はすべてのファイルを元のツリーへ戻し、台帳から画像を外します。次の実行から
各ステージにまた見えるようになります。

重複、失敗したクロップ、単にこの LoRA の題材ではない絵 — 削除したあとで後悔しそうなものに
使ってください。Apply ゲートも実行バーの Undo もありません。除外すること*そのもの*が操作で、
戻すことがその逆操作です。

---

## 7. ステージごとのワークフロー

上から順に実行してください。各ステージはリサイズ済みツリーを読むため、画像が Resize を通るのは一度だけで、
各キャプションステージは前のステージが書いた改訂版キャプションを読みます。

### 7.1 Resize（自動）

読み込み `(source_dir)/`。書き込み `workspace/resized/<rel>.png`。

各画像は、最も小さいリサイズで済むバケット階層に、その階層のトークン帯の中で元のアスペクト比を保ったまま収まります。
ジオメトリは決定的なので、再実行してもすべての画像がすでにバケットにあると判断してスキップされます。
ピクセル下限（デフォルト 0.5 MP）未満の画像はスキップされ、レポートに名前が挙げられます。
そのような画像は決してリサイズされないため、どのステージからも見えません。
画像パネルのピクセル数チップにその旨が表示され、下限は ⚙ Advanced settings › Preprocess にあります。

これをクリックすることはありません。GUI はリサイズ済みツリーを読むすべてのステージの最初のステップとしてこれを実行し、
1 枚の画像に対する実行はその画像だけをリサイズします。すでに最新の画像はスキップされるため、プリフライトのコストはほぼゼロです。
常に書き込みます。ドライランはありません。

> 階層（`target_res`）は学習パイプラインが期待するものと一致していなければなりません。
> さもないと双方が互いの出力をリサイズし続けます。

### 7.2 Autotag captions（キャプションの自動タグ付け）

読み込み リサイズ済みの各画像とそのキャプション（改訂版、無ければマスター）。書き込み 改訂版キャプション。
モデル: Anima Tagger（タガーチェックポイント + ゲーテッドの dbv4 バックボーン）。

画像ごとに Anima 順のタグ文字列（`rating, count, characters, copyrights, @artists,
generals`）を予測します。3 つのモードがあります:

| モード | 動作 |
|---|---|
| `missing`（デフォルト） | どのキャプションも語っていない画像だけ。既存のものには触れません。 |
| `merge` | キャプションに無いタグを追記し、位置節は保ちます。 |
| `overwrite` | キャプションを丸ごと置き換えます。古いテキストは history バージョンとして残ります。 |

非破壊なのは `missing` だけです。他の 2 つは history の段と Undo で元に戻せます。
出力はあくまで出発点として扱い、学習前に名前、シリーズ、アーティストを必ず確認してください。
タガー自体については [`docs/anima_tagger.md`](../../../docs/anima_tagger.md) を参照してください。

### 7.3 Correct + mirror captions（キャプションの補正とミラー）

読み込み マスターキャプションと Danbooru タグ KB。書き込み 改訂版キャプションと、任意で `.variants.txt`。

各マスターを補正済みの改訂版キャプションにミラーします。タグは KB に照らして型付けされ Anima のバケットに並べ替えられ、
任意のトリガーワードが差し込まれ、任意の `@no-artist` センチネルが付き、
任意のドロップグループがマスターに触れずにミラーされた全キャプションからタグファミリーごと取り除きます。
バリアント数を設定すると `<rel>.variants.txt` も書きます。`v0` は補正済みキャプション、
`v1…` はスマートシャッフルとドロップアウトの抽選で、下流はこれをそのままエンコードします。

常に書き込みます。ドライランもレポートも無いため Undo もありません。置き換えられたテキストは history の段に残ります。
KB は Models の Danbooru tag KB 行です（§5）。任意の English 行は、タグ検索が表示する説明文を書き換えます。

> Correct がミラーするのはマスターです。改訂版キャプションの位置節は再実行しても生き残りますが
> （新しいミラーに再付加されます）、Autotag `merge` がフラットバッグに追加したタグは生き残りません。
> そのため Correct は Autotag `merge` の後ではなく前に実行してください。

### 7.4 Position captions（位置キャプション）

読み込み リサイズ済み画像とそのキャプション。書き込み 改訂版キャプション。
モデル: SAM 3（ゲーテッド）、被写体ソフトプロンプト、Anima Tagger。

被写体が 2 つ以上ある画像について、それぞれを検出し、読み順に並べ、マスクで空白化した各クロップにタグ付けし、
キャプションを位置節文法 `<flat tag bag>. On the left, …. On the right, ….` に書き換えます。
1 つの被写体に属するタグはフラットバッグからその被写体の節へ移動します。
キュレーション済みキャプションが一度も述べていないことを、わずかな許容量を超えて作り出すことはありません。
被写体が 1 つの画像はスキップされ、レポートに理由が記されます。

適用は、書き換えたキャプションの横にある古い `.variants.txt` を削除します。
取り消しは専用のモード `--flatten` が担い、モデルをロードせずにすべての節をフラットバッグへ戻します。Undo でも構いません。
完全な文法、ゲート、すべてのノブは [`docs/position_captions.md`](../../../docs/position_captions.md) にあります。

### 7.5 Multiview audit（マルチビュー監査）

専用のボタンはありません。位置ステージの最初のフェーズとして実行され、そのフォームの
`multiview_audit` で設定します（`report` は監査だけでタグは何も付けず、`apply` は採用された所見を
同じ実行のスイープにも渡します）。CLI からは今も単独で実行できます。

読み込み Position ステージがスキップした単一被写体の画像。書き込み 改訂版キャプション。
書き加えるのはフラットバッグ末尾の `multiple views` だけです。モデル: Position と同じ。

実際には 1 人の少女の複数ビューである `1girl` 画像を見つけ、
それぞれをレポートディレクトリ以下のコンタクトシートとともに報告します。
適用はデフォルトで強い所見に限定されます。弱い所見の裏付けはジオメトリだけなので、まずシートを確認してください。

> 手書きのマスターには決して触れません。適用が置き換えたテキストは history バージョンとして残り、
> Undo がレポートを再生します。[`docs/multiview_audit.md`](../../../docs/multiview_audit.md) を参照してください。

### 7.6 OCR text（画像内テキストの OCR）

読み込み リサイズ済み画像。書き込み `workspace/ocr/<rel>.ocr.txt`。
モデル: AnimeText テキストブロック検出器（吹き出しのセリフと、絵の上に描かれた効果音を
まとめて見つけます）と、マンガ VL リーダー（見つけた枠をすべて読みます。日本語、中国語、英語に対応し、
ハート、小書き仮名、手描きの効果音も読み取ります）。どちらも Models & weights の OCR パックです。

画像の中にある言葉、つまりセリフ、看板、効果音を、テキストブロックごとに位置とともに 1 行ずつ記録します。
これはキャプションではありません。下流の何もこれをエンコードせず、キャプションの読み書きは無く、
後で再エンコードも不要です。画像パネルに各行が表示されます。
デフォルトでは ASCII のみの行（ページ番号、URL）は捨てられます。
吹き出しは 1 ブロックとして扱われるため、その中の各列はまとめて 1 行として届きます。

### 7.7 Build groups（グループの構築）

読み込み リサイズ済み画像。書き込み `workspace/groups/groups.json`。モデル: PE-Spatial。

ほぼ同一の画像（重複、別バージョン、1 枚の絵のクロップ）を、ファイル名やキャプションではなく
視覚的な内容にもとづいて、最上位フォルダ単位でクラスタリングします。サイドバーの groups 順序がその結果を描きます。
グループごとに折りたたみ可能なヘッダーが 1 つあり、冗長なものが一か所に集まるので間引きが容易です。
フィルターと保留中のドットは、どちらの順序でも同じ意味です。

再実行のコストは低く、特徴量は `~/.cache/near_twin/` 以下（`NEAR_TWIN_CACHE` で上書き可能）に
ファイルのサイズと mtime のスタンプ付きでキャッシュされるため、再リサイズしても変わった画像だけが正確に再計算されます。
クラスタリングの厳しさはフォームの 2 つのマッチしきい値で調整します。[`docs/grouping.md`](../../../docs/grouping.md) を参照してください。

### 7.8 マスク: 設定・統合

読み込み リサイズ済み画像。書き込み `workspace/masks_sam/` と、そのマージを `workspace/masks/` 以下に。
いずれもソースのサブフォルダ構成を写した 8 ビットの `{stem}_mask.png` です。モデル: SAM 3。

- 設定はマスクの一覧で、1 行が 1 つのマスクです。各行で役割、つまり **残す**（残した領域だけを
  学習します）か **除外**（この領域は損失から外れます）と、種類、つまり **テキスト**
  （`speech bubble` のような SAM3 プロンプト）か **ソフト**（学習済みプロンプトファイル、
  `.safetensors`）を選びます。+ で行を追加し、× で行を外します。
  デフォルトは同梱のソフトプロンプトで被写体を残す 1 行です。吹き出しや文字も、
  ここではただの除外の行にすぎません。
- Merge は入力ツリーのピクセルごとの最小値を `workspace/masks/` に取ります。
  サイドバーが mask / overlay として表示し、Export が公開するツリーです。
  デフォルトの入力はジェネレーターのツリーで、入力が存在しなければスキップされ、
  手描きのツリーをその隣に列挙することもできます。

2 つのディレクトリは 2 つのフィールドではなく 1 つの設定です。
ジェネレーターのツリーの隣にマージされる 2 つ目のツリーがあれば、マスクに同じ名前を付けてしまい、
共有ツリーで互いに上書きしてしまうからです。マスクは常に書き込みます。変更するには再生成してください。
詳しくは [`docs/masking.md`](../../../docs/masking.md) を参照してください。

### 7.9 Export workspace（ワークスペースのエクスポート）

読み込み ワークスペース。書き込み `(export_target_dir)/`。改訂されたマスターがあれば
`(source_dir)/` にも書きます。

ワークスペースの外に書く唯一のステージです。6 種類の成果物（リサイズ済み画像、改訂版キャプション、
バリアントサイドカー、マスク、改訂されたマスター、キャプションインデックス）を公開し、
それぞれを移動先と個別に照らして判断します。
同一のファイルはスキップされる（テキストはバイト比較、ピクセルはサイズと mtime）ため、
変更のないデータセットの再エクスポートは走査と stat が 1 回ずつで済みます。
常にコピーし、リンクは決して作らないため、ワークスペースを消してもエクスポートツリーは残ります。

CLI からはデフォルトでドライランになり、コピーする予定のものを一覧します。GUI では Run がコピーし、
Undo はエクスポート自身の台帳から上書きしたテキストを復元します。
上書きされたピクセルは復元できず、その旨が報告されます。ワークスペースの外へ出るのは Export が書いたものだけです。

画像単位の Run を持たず、`path_pattern` も受け取らない唯一のステージです。公開は常に
ワークスペース全体になります。1 枚だけを公開すると、トレーナーが今なおデータセット全体として読む
ツリーの隣に置かれることになり、Export を押したときに期待するものとは正反対です。

---

## 8. CLI での同等コマンド

すべてのステージは、GUI のフォームが埋めるのと同じリクエストオブジェクトの上に載った `python -m` モジュールで、
フォームのフィールド 1 つにつきフラグが 1 つあります（`--help` で一覧できます）。パスはキュレーションホームからの相対です。
キャプションステージはフラグをアンダースコアで（`--path_pattern`）、グループ化とマスキングはハイフンで
（`--source-dir`）綴りますが、どちらの綴りもすべての場所で受け付けられます。

| ステージ | モジュール | `--apply` 無しで書き込む? |
|---|---|---|
| Resize | `anime_tools.stages.cli.resize_images` | 常に書き込む |
| Autotag | `anime_tools.stages.cli.autotag_captions` | ドライラン → `report.json` |
| Position | `anime_tools.stages.cli.position_captions` | ドライラン → `report.json` |
| Correct | `anime_tools.stages.cli.correct_captions` | 常に書き込む |
| Audit | `anime_tools.stages.cli.audit_multiview` | ドライラン → `report.json` |
| OCR | `anime_tools.stages.cli.ocr_captions` | ドライラン → `report.json` |
| Groups | `anime_tools.grouping.cli.build_groups` | 常に `groups.json` を書き込む |
| SAM3 masks | `anime_tools.masking.cli.generate_masks` | 常に書き込む |
| Merge masks | `anime_tools.masking.cli.merge_masks` | 常に書き込む |
| Export | `anime_tools.stages.cli.export_workspace` | ドライラン → `report.json` |
| 除外（§6.5） | `anime_tools.exclude` | ドライラン → 何が動くかを表示 |

ドライランのステージは `workspace/captions/<stage>/` 以下に `report.json` を書いて停止します。
`--apply` で実際に書き込みます。Position captions と Multiview audit は `--from_report <report.json>` も受け取り、
これはドライランの提案をモデルをロードせずに再生し、その間に変更されたキャプションはスキップして、
読んだレポートの横に `apply_report.json` を書きます。Autotag にこのフラグはありません。
このステージの処理は画像ごとに順伝播が一度きりで、何をスキップするか判定するより再実行するほうが安上がりだからです。
GUI の Undo は同じ再生を逆向きにしたものです。

```bash
python -m anime_tools.stages.cli.autotag_captions --mode merge            # dry run
python -m anime_tools.stages.cli.autotag_captions --mode merge --apply    # write
python -m anime_tools.stages.cli.export_workspace --apply                 # publish

python -m anime_tools.exclude char_aki/bad.jpg --note "bad crop" --apply  # take one out
python -m anime_tools.exclude --list                                     # what is out
python -m anime_tools.exclude char_aki/bad.jpg --restore --apply         # put it back
```

Python API も同じオブジェクトです。[`examples/`](../../../examples/README.md) には機能ごとに実行可能なスクリプトが
1 つずつあり、CLI と並べて API を示しています。

---

## 9. アップデート

```bash
uv tool upgrade anime-tools          # tool install
uv lock --upgrade-package anime-tools   # as a dependency
```

GUI はコミット済みのバンドルなので、アップデートにフロントエンドのビルドは不要です。
ステージのフォームはインストールされたコードから生成されるため、新しいフラグはフォームに自動的に現れます。

---

## 10. トラブルシューティング

ステージがゲーテッドリポジトリのエラー（Hub からの 401 / 403）で失敗する。
トークンが無いか、アカウントがそのリポジトリの利用規約に同意していません。⚙ Settings でトークンを設定してから、
Models & weights の該当行の accept the terms リンクを、同じアカウントでサインインした状態で開いてください。
Download ボタンはステージをフル実行せずにこの失敗を再現できます。

自分のプロジェクトで `uv` が numpy の解決に失敗する。SAM3 の古い `numpy<2` 固定が原因です。
§3.2 のオーバーライドを `pyproject.toml` に追加してください。

macOS: `import sam3` が triton について文句を言う、またはテンソルを `cuda` に置けない。
どちらもパッケージの SAM3 ローダー内で処理されています。
これらが見えるなら、sam3 を自分でインポートしているか、古いリリースを実行しています。
macOS では SAM3 ステージは CPU で、ゆっくりと動きます。

Windows: torch に CUDA が無い。PyPI の Windows 向け torch は CPU 専用です。
CUDA インデックスをデフォルトとする PowerShell インストーラーで再インストールするか、`TORCH_INDEX` を明示的に設定してください。

ステージが画像を見つけない / この画像に対して何もしない。その画像が `workspace/resized/` 以下にありません。
リサイズの下限未満か（ピクセル数チップにそう表示されます。⚙ Advanced settings › Preprocess で下限を下げてください）、
`src` ルートがデータセットを指していないかのどちらかです。

「saved — .variants.txt is now stale」。バリアントを持つ改訂版キャプションを編集したときに出ます。
同じバリアント数で Correct を再実行し、その後で下流のテキスト埋め込みを再エンコードしてください。

Undo が何かをスキップしたと言う。キャプションが実行の書いた内容をもう保持していないため
（自分で編集したか、後の実行が変更したためです）、その行はそのまま残されました。
スキップされた行はレポートにきちんと記録されています。

実行が間違ったレポートを読む、または 2 つのステージが 1 つのレポートを共有する。
各ステージはレポートルート以下に自分のディレクトリ（`captions/autotag`、`captions/position`、…）を持ちます。
ルートは 1 つの Settings 値で、それを動かせばすべてが動きます。空のままにするとレポートは `dst` ルートの横に置かれます。

---

## 11. 追加資料

- [`docs/anima_tagger.md`](../../../docs/anima_tagger.md)：タガー、その語彙とキャリブレーション。
- [`docs/position_captions.md`](../../../docs/position_captions.md)：節の文法、ゲート、ノブ。
- [`docs/multiview_audit.md`](../../../docs/multiview_audit.md)：監査の判定とシート。
- [`docs/grouping.md`](../../../docs/grouping.md)：近縁（near-twin）グループ化。
- [`docs/masking.md`](../../../docs/masking.md)：被写体マスクとそのマージ。
- [`examples/README.md`](../../../examples/README.md)：Python API、機能ごとに 1 スクリプト。
