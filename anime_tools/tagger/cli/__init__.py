"""Tagger CLIs. ``python -m anime_tools.tagger.cli --mode <mode>`` (vocab build /
predict / curation modes), ``…cli.autotag`` (one image), ``…cli.autotag_server``
(resident worker; the GUI + node speak its stdout protocol),
``…cli.build_dbv4_ckpt``, ``…cli.train_sidecar`` (GPU)."""
